This code is the next part of MS5 it consist of more functionality addition to MS5_3.
Along with the functions previously defined it contains
A stockItem method that allows the user to update the quantity of an item in the inventory, It calls the selectItem method that prompts the user to select an item from the inventory it ensures the validated entry from the user. Then it displays the data get from that particular selection in POS_FORM. Then it asks quantity from the user that is also validated and it keeps asking the user until user enters the valid value. At the end it sets the quantity data member in Item class
